{
  "name": "ableton-stream-deck-rack-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "maxforlive:sync": "node scripts/sync-maxforlive.mjs",
    "harness:dev-live-host": "node --experimental-strip-types src/harness/dev-live-host.ts",
    "harness:fake-ableton": "node --experimental-strip-types src/harness/fake-ableton-client.ts"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0",
    "ws": "^8.20.1"
  }
}
